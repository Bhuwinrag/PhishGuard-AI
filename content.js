// This script runs in the context of the web page.
// Its primary purpose is to be a target for the chrome.scripting.executeScript
// call from the background script, allowing the extension to interact with the DOM.
console.log("PhishGuardAI content script loaded and ready.");